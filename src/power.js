//creating the class for power capsule

class Capsule{
    constructor(capsuleHight, capsuleWidth, capsuleType) {
        this.capsuleHight = capsuleHight,
        this.capsuleWidth = capsuleWidth,
        this.capsuleType = capsuleType
    }
}